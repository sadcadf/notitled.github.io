{
    "name": "notitled-blog",
    "version": "2.0.0",
    "description": "Минималистичный личный блог",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build && node scripts/generate.js --outDir dist",
        "preview": "vite preview",
        "generate": "node scripts/generate.js",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "typescript": "^5.3.0",
        "vite": "^7.3.1",
        "vite-plugin-pwa": "^1.2.0"
    },
    "dependencies": {
        "marked": "^17.0.2"
    }
}
