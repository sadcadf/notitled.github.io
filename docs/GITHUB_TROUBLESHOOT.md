# 🔍 Проблема: GitHub репозиторий не найден

## Возможные причины:

### 1. Username написан с маленькой буквы

**Проверь свой GitHub username:**
- Открой https://github.com/settings/profile
- Посмотри "Username"

**Если username `notitled` (с маленькой буквы), то:**

```bash
cd /Users/dimailev/anti/нд/blog

# Удали неправильный remote
git remote remove origin

# Добавь правильный (с маленькой буквы)
git remote add origin https://github.com/notitled/notitled.github.io.git

# Попробуй запушить
git push -u origin main
```

---

### 2. Репозиторий создан с другим именем

**Проверь что репозиторий называется ТОЧНО:**
```
notitled.github.io
```

Или может быть `Notitled.github.io`? GitHub чувствителен к регистру!

---

### 3. Нужна аутентификация

**Если просит логин/пароль:**

GitHub больше не принимает пароли для push! Нужен **Personal Access Token**.

#### Создай токен:

1. Открой: https://github.com/settings/tokens/new
2. Note: `Blog deploy`
3. Expiration: `No expiration` (или выбери срок)
4. Scopes: отметь `repo` (все галочки под ним)
5. Generate token
6. **СКОПИРУЙ ТОКЕН!** (показывается один раз)

#### Используй токен:

```bash
# При запросе пароля вставь токен вместо пароля
git push -u origin main
```

**Или используй SSH (рекомендую):**

```bash
# Создай SSH ключ (если нет)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Скопируй публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавь на GitHub:
# https://github.com/settings/keys → New SSH key → вставь ключ

# Смени remote на SSH
git remote remove origin
git remote add origin git@github.com:notitled/notitled.github.io.git

# Пуш
git push -u origin main
```

---

## 🎯 ЧТО ДЕЛАТЬ СЕЙЧАС:

1. **Проверь свой username** на https://github.com/settings/profile
2. **Скажи мне точный username** (с учетом регистра)
3. Я запущу команды с правильным именем!

---

**Какой у тебя точный username на GitHub?** 
(например: `Notitled` или `notitled`)
