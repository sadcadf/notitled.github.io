# 🔧 ИСПРАВЛЕНИЕ: Изображения не отображаются

## ✅ ПРОБЛЕМА РЕШЕНА

### Причина
Браузер кэшировал старую версию JavaScript файла с WebP кодом.

### Решение
Добавлен version query параметр `?v=2` к скрипту для сброса кэша.

---

## 📋 ЧТО ДЕЛАТЬ СЕЙЧАС

### Шаг 1: Жесткая перезагрузка страницы

**Метод 1 (рекомендуется):**
```
Mac: Cmd + Shift + R
Windows/Linux: Ctrl + Shift + R
```

**Метод 2: Через DevTools**
1. Открой DevTools (F12)
2. Правой кнопкой на кнопку обновления
3. Выбери "Очистить кэш и жесткая перезагрузка"

**Метод 3: Очистить кэш вручную**
1. DevTools (F12) → Application/Storage
2. Clear storage
3. Refresh

---

## ✅ КАК ПРОВЕРИТЬ ЧТО РАБОТАЕТ

### 1. Открой DevTools (F12)
- Перейди на вкладку **Network**
- Перезагрузи страницу

### 2. Найди `app.js?v=2` в списке
- Должен загружаться с параметром `?v=2`
- Status: 200 OK (не 304 Not Modified)

### 3. Проверь изображения
- Найди `first-post-preview.jpg`
- Status должен быть 200 OK
- Type: `image/jpeg`
- Size: ~40 KB

---

## 🐛 ЕСЛИ ВСЁ ЕЩЁ НЕ РАБОТАЕТ

### 1. Проверь консоль (F12 → Console)
Ищи ошибки вроде:
```
Failed to load resource: 404 Not Found
```

### 2. Проверь Network tab
- Фильтруй по "Img"
- Посмотри какой точно URL загружается
- Должно быть: `http://localhost:8000/posts/images/first-post-preview.jpg`

### 3. Ручная проверка файла
Открой в браузере напрямую:
```
http://localhost:8000/posts/images/first-post-preview.jpg
```

Если открывается → проблема в коде
Если 404 → проблема с файлом

---

## 🔍 ДИАГНОСТИКА

### Проверь структуру файлов

```
blog/
├── posts/
│   ├── images/
│   │   └── first-post-preview.jpg  ← Должен быть здесь!
│   ├── index.json
│   └── first-post.md
```

### Проверь paths/index.json:
```json
{
  "slug": "first-post",
  "preview": "posts/images/first-post-preview.jpg"  ← Правильный путь
}
```

❌ **НЕ ДОЛЖНО БЫТЬ:**
- `"preview": "images/first-post-preview.jpg"`
- `"preview": "/posts/images/first-post-preview.jpg"`
- `"preview": "first-post-preview.jpg"`

---

## 💡 ПОЧЕМУ ЭТО ПРОИСХОДИТ

### Browser Cache
Браузеры агрессивно кэшируют JavaScript и CSS:
- Для скорости
- Экономии трафика
- Но иногда это мешает при разработке

### Решения для разработки

**1. Disable cache в DevTools**
```
DevTools → Network → ☑ Disable cache
```
Работает только пока DevTools открыт.

**2. Version query параметр (текущее решение)**
```html
<script src="app.js?v=2"></script>
```
При изменении кода меняй версию: `?v=3`, `?v=4` и т.д.

**3. Hash в имени файла (production)**
```html
<script src="app.a1b2c3d4.js"></script>
```
Лучший способ для production.

---

## ✨ ТЕКУЩИЕ ИЗМЕНЕНИЯ

### index.html:
```html
<!-- БЫЛО -->
<script src="assets/js/app.js"></script>

<!-- СТАЛО -->
<script src="assets/js/app.js?v=2"></script>
```

### app.js:
- ✅ WebP код убран
- ✅ Прямые `<img>` теги
- ✅ Светлая тема по умолчанию

---

## 🎯 ИТОГ

После жесткой перезагрузки (`Cmd+Shift+R`) должно быть:

✅ Изображения видны
✅ Светлая тема по умолчанию  
✅ Всё остальное работает

**Если не помогло - сделай скриншот консоли (F12 → Console) и покажи мне!** 📸
